# FrameLearn 环境变量模板
# 复制为 .env 后填写真实凭据；不要提交 .env。

# ── 文字 API ─────────────────────────────────────────────────────
# CommandParser 在 TEXT_PROVIDER + 有效 TEXT_API_KEY 同时存在时使用文字 API。
# runtime.text_mode = "api" 时，ask 也使用这里的配置。
# provider 可选：gemini / deepseek / openai / claude / openrouter / kimi / zhipu / siliconflow
# TEXT_PROVIDER=deepseek
# TEXT_API_KEY=your_text_api_key_here
# TEXT_MODEL=deepseek-chat
# TEXT_BASE_URL=https://api.deepseek.com/v1/

# ── 文档 Vision API ──────────────────────────────────────────────
# DocumentGenerator 的 provider/model 以 settings.toml 的
# runtime.vision_provider / runtime.vision_model 为准。
# 非 SiliconFlow provider 使用 VISION_API_KEY；可用 VISION_BASE_URL 覆盖 endpoint。
# VISION_API_KEY=your_vision_api_key_here
# VISION_BASE_URL=https://example.com/v1/

# 当前 settings.toml 默认使用 SiliconFlow Vision API，同时此 key 也用于
# asr.provider = "siliconflow" 时的 SenseVoice 转写。
SILICONFLOW_API_KEY=your_siliconflow_key_here
# SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1/

# ── DashScope 长音频 ASR ─────────────────────────────────────────
# 当前 settings.toml 默认 asr.provider = "dashscope"。
DASHSCOPE_API_KEY=your_dashscope_key_here

# DashScope filetrans 通过签名 URL 拉取音频，因此还需要 OSS 凭据。
# Bucket、region、prefix 和 URL TTL 在 settings.toml 的 [asr.oss] 配置。
OSS_ACCESS_KEY_ID=your_oss_access_key_id
OSS_ACCESS_KEY_SECRET=your_oss_access_key_secret

# ── Codex app-server ─────────────────────────────────────────────
# runtime.text_mode 或 runtime.vision_mode 为 appserver 时，不从本文件读取
# Codex API key；FrameLearn 启动本机 `codex app-server`，认证与模型由 Codex CLI
# 自己的配置负责。
